export type AppThemeColors = {
  rootBg: string;
  screenBg: string;
  border: string;
  borderStrong: string;
  text: string;
  textSecondary: string;
  textMuted: string;
  accent: string;
  cardBg: string;
  segmentBg: string;
  segmentActiveBg: string;
  segmentText: string;
  segmentTextActive: string;
  toggleBg: string;
  toggleOnBg: string;
  toggleThumb: string;
  modalOverlay: string;
  modalBg: string;
  modalBorder: string;
  inputBg: string;
  inputPlaceholder: string;
  metricIconBg: string;
  hourCardActiveBg: string;
  hourActiveText: string;
  backIcon: string;
  itemIcon: string;
  footerText: string;
};

export const darkTheme: AppThemeColors = {
  rootBg: '#cbd5e1',
  screenBg: '#0f172a',
  border: 'rgba(255, 255, 255, 0.12)',
  borderStrong: '#111827',
  text: '#f9fafb',
  textSecondary: 'rgba(249, 250, 251, 0.6)',
  textMuted: 'rgba(249, 250, 251, 0.5)',
  accent: '#38bdf8',
  cardBg: 'rgba(255, 255, 255, 0.08)',
  segmentBg: 'rgba(15, 23, 42, 0.6)',
  segmentActiveBg: '#f9fafb',
  segmentText: '#e5e7eb',
  segmentTextActive: '#0f172a',
  toggleBg: 'rgba(255, 255, 255, 0.1)',
  toggleOnBg: '#38bdf8',
  toggleThumb: '#f9fafb',
  modalOverlay: 'rgba(0, 0, 0, 0.5)',
  modalBg: '#1e293b',
  modalBorder: 'rgba(255, 255, 255, 0.12)',
  inputBg: 'rgba(255, 255, 255, 0.08)',
  inputPlaceholder: 'rgba(249, 250, 251, 0.6)',
  metricIconBg: 'rgba(56, 189, 248, 0.1)',
  hourCardActiveBg: '#38bdf8',
  hourActiveText: '#0f172a',
  backIcon: '#f9fafb',
  itemIcon: 'rgba(249, 250, 251, 0.9)',
  footerText: 'rgba(249, 250, 251, 0.3)',
};

export const lightTheme: AppThemeColors = {
  rootBg: '#e2e8f0',
  screenBg: '#ffffff',
  border: 'rgba(0, 0, 0, 0.08)',
  borderStrong: '#cbd5e1',
  text: '#0f172a',
  textSecondary: '#475569',
  textMuted: '#64748b',
  accent: '#0284c7',
  cardBg: '#f8fafc',
  segmentBg: '#e2e8f0',
  segmentActiveBg: '#0f172a',
  segmentText: '#64748b',
  segmentTextActive: '#f8fafc',
  toggleBg: '#e2e8f0',
  toggleOnBg: '#0284c7',
  toggleThumb: '#ffffff',
  modalOverlay: 'rgba(0, 0, 0, 0.4)',
  modalBg: '#ffffff',
  modalBorder: '#e2e8f0',
  inputBg: '#f1f5f9',
  inputPlaceholder: '#94a3b8',
  metricIconBg: 'rgba(2, 132, 199, 0.12)',
  hourCardActiveBg: '#0284c7',
  hourActiveText: '#ffffff',
  backIcon: '#0f172a',
  itemIcon: 'rgba(15, 23, 42, 0.85)',
  footerText: 'rgba(15, 23, 42, 0.4)',
};
